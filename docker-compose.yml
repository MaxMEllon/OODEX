version: '2'
services:
  db:
    image: postgres
  app:
    build: .
    env_file: .env
    command: rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/TEE
    ports:
      - 3000:3000
    depends_on:
      - db
      - cache
  cache:
    image: redis
